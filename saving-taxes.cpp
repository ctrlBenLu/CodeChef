#include <bits/stdc++.h>
using namespace std;

void solve() {
    int y, x; cin >> y >> x;

    cout << y-x << endl;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t; cin >> t;

  while (t--) solve();

  return 0;
}